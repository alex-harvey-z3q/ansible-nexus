---
- name: Reload systemd
  ansible.builtin.systemd:
    daemon_reload: true
  become: true

- name: Restart nexus
  ansible.builtin.systemd:
    name: "{{ nexus_service_name }}"
    state: restarted
  become: true
